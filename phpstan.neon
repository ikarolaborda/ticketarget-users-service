# Baseline holds pre-adoption inference debt only — new errors still fail.
includes:
    - phpstan-baseline.neon
    - vendor/larastan/larastan/extension.neon
parameters:
    level: 6
    paths:
        - app
